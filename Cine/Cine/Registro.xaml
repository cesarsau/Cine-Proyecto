<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Cine.Registro"
             BackgroundColor="#54687a"
             NavigationPage.HasNavigationBar="true">
    <ContentPage.Content>
        <Grid>
            <Image Source="fondosala.jpg"
               HeightRequest="250" Aspect="AspectFill" Opacity="0.2"/>

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid>
                    
                </Grid>
                <StackLayout Grid.Row="1" Padding="30,20">
                    <Label Text="Registro" TextColor="White" FontSize="Title"></Label>
                    <Label x:Name="Total" TextColor="White" ></Label>
                    <Entry x:Name="nombre" Placeholder="Nombre y Apellido" PlaceholderColor="WhiteSmoke" TextColor="White" Keyboard="Text"/>
                    <Entry x:Name="telefono"  Placeholder="Telefono" PlaceholderColor="WhiteSmoke" TextColor="White" Keyboard="Telephone"/>
                    <Entry x:Name="correo" Placeholder="Correo electronico" PlaceholderColor="WhiteSmoke" TextColor="White" Keyboard="Email"/>
                    <Entry x:Name="ciudad" Placeholder="Ciudad" PlaceholderColor="WhiteSmoke" TextColor="White" Keyboard="Text"/>
                    <Entry x:Name="direccion" Placeholder="Direccion" PlaceholderColor="WhiteSmoke" TextColor="White" Keyboard="Text"/>
                    <Entry x:Name="contraseña" Placeholder="Contraseña" PlaceholderColor="WhiteSmoke" IsPassword="True" TextColor="White"/>
                    <Entry x:Name="confirmar" Placeholder="Confirme Contraseña" PlaceholderColor="WhiteSmoke" IsPassword="True" TextColor="White" />
                    <Button Clicked="Button_Clicked" Text="Registrarme" BackgroundColor="#c56f82" CornerRadius="5" TextColor="WhiteSmoke" FontSize="15"/>
                    <Grid>

                    </Grid>

                    <Grid >
                        <Image Source="icono.ico" />
                    </Grid>
                    
                </StackLayout>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>